class Event {
  final String id;
  final String title;
  final String whatToExpect;
  final String eventPlan;
  final String sessions;

  const Event(
      this.id, this.title, this.whatToExpect, this.eventPlan, this.sessions);
}
